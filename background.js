const hoveredTab = () => {
    // implement a method that holds the hovering logic
    // returns the hovered tab and potentially the content that needs to be summarized. 
}

const summaryGenerator = () => {
    // this method will engage with the ai and return a  summary of the tab
}

const summaryDisplayer = () => {
    // this will hold the logic to display the summarized info.
}